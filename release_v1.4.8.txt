v1.4.8 Release Notes
- Stability: Optimized prompt rules to ensure the LLM outputs verbs and their auxiliary verb conjugations as a single token (e.g. "いかない", "限られる") rather than splitting them into separate lines.
